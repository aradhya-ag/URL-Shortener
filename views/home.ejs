<!DOCTYPE html>
<html>
<head>
    <title>URL Shortener</title>
    <style type="text/css">
        @import url(https://fonts.googleapis.com/css?family=Asap:400,700|Lato:400,700,900|Source+Sans+Pro:400,700&display=swap);
        body {
            margin:0;
            background:#f9f9f9;
            font:14px asap,arial;
            margin-bottom:200px
        }
        a {
            color:#006cff;
            text-decoration:none
        }
        a:hover {
            text-decoration:underline
        }
        br {
            clear:both
        }
        .logo {
            font:bold 40px arial,tahoma;
            color:#222;
            word-wrap:break-word;
            cursor: pointer;
        }
        header {
            text-align:center
        }
        header #top {
            background:#fff;
            box-shadow:0 1px 4px #ccc
        }
        header #logo {
            padding:40px 20px 25px
        }
        header a.logo {
            font:900 49px asap,arial;
            color:#0186da;
            letter-spacing:-1px;
            text-shadow:0 2px 2px #ddd;
            word-wrap:break-word
        }
        header a.logo:hover {
            text-decoration:none
        }
        main {
            display:block;
            margin:0 auto 0 auto;
            padding:0 20px;
            max-width:1000px
        }
        section#urlbox {
            margin:0 auto 20px auto;
            max-width:758px;
            box-shadow:0 1px 4px #ccc;
            border-radius:2px;
            padding:10px 30px 20px;
            background:#fff;
            text-align:center
        }
        section#content {
            max-width:720px;
            margin:30px auto 20px;
            padding:0 0 20px 0
        }
        footer {
            text-align:center;
            font:17px "source sans pro",arial;
            color:#eee;
            background:#333;
            width:100%;
            border-top:4px solid #00a0db;
            padding:20px 0 90px;
            line-height:1.5;
            margin:40px 0 0 0;
            position:absolute;
            bottom:0;
            height:40px
        }
        html {
            position:relative;
            min-height:100%
        }
        * {
            box-sizing:border-box;
            -webkit-box-sizing:border-box;
            -moz-box-sizing:border-box
        }
        .buttons {
            margin-top: 20px;
        }
        button {
            padding: 12px 24px;
            font-size: 18px;
            border: none;
            border-radius: 4px;
            color: #fff;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button.search-button {
            background-color: #007bff;
        }
        button.shorten-button {
            background-color: #28a745;
            margin-left: 10px;
        }
        button:hover {
            background-color: #0056b3;
        }
        button:active {
            background-color: #002c5c;
        }
        .background-image {
            background-image: url('views\images\17973908.jpg');
            background-size: cover;
            width: 200px;
            height: 100vh;
        }
    </style>
    <script>
        function goToShortUrl() {
            window.location.href = '/ShortUrl';
        }
        function goToSearch() {
            window.location.href = '/Search';
        }
        function redirectToHome() {
            window.location.href = '/';
        }
    </script>
</head>
<body>
    <header>
        <div id="top">
            <div id="logo">
                <a onclick="redirectToHome()" class="logo">URL Shortener</a>
            </div>
        </div>
    </header>
    <main>
        <section id="content">
            <br>
        </section>
        <section id="urlbox">
            <h3>Welcome to the ultimate URL Shortener. What task would you like to perform?</h3>
            <button class="shorten-button" onclick="goToShortUrl()">Shorten URL</button>
            <button class="search-button" onclick="goToSearch()">Search for URL</button>
            <br>
        </section>
    </main>
    <footer>
        <div id="footerbox">
            <div>A Tool to shorten a long link</div>
        </div>
    </footer>
</body>
</html>
